# shellcheck shell=dash
# vim: syntax=sh:

. '/lib/live/config/common.logging'

# Spawn pnmixer
if [ -x /usr/bin/pnmixer ] ; then
	pnmixer &
	logtty 'spawned pnmixer.'
else
	logtty 'Not spawning pnmixer - executable not found.'
fi
