# shellcheck shell=dash
# vim: syntax=sh:

. '/lib/live/config/common.logging'

# Spawn tint2
if [ -x /usr/bin/tint2 ] ; then
	tint2 &
	logtty 'spawned tint2.'
else
	logtty 'Not spawning tint2 - executable not found.'
fi
